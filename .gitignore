# Historical price data (ใหญ่มาก ~500MB)
data/
files/
*.zip

# Backtest CSVs (optional — uncomment ถ้าไม่อยากเก็บใน repo)
# backtest_results_*.csv
# backtest_v5_*.csv

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Logs (ไม่จำเป็นต้อง track)
cron.log

# trade_data.db (old DB, ใช้ paper_trades.db แทน)
trade_data.db

# Env files
.env
